name: Zeffo Test

on:
  workflow_call:
    inputs:
      ref:
        required: true
        type: string
      skipTests:
        type: boolean
        required: false
        default: false

jobs:
  test:
    runs-on: ubuntu-18.04
    if: ${{ ! inputs.skipTests }}
    steps:
      - run: |
          echo ${{ inputs.ref }}
          echo ${{ inputs.environment }}
          echo ${{ inputs.release_version }}
