name: Deploy a build

on:
  push:
  workflow_dispatch:
    inputs:
      tag:
        required: true
        type: string

jobs:
  test:
    runs-on: ubuntu-18.04
    steps:
      - id: vars
        uses: ./.github/actions/zeffoEnv
      - run: |
          echo ${{ steps.vars.outputs.ref }}
          echo ${{ steps.vars.outputs.environment }}
          echo ${{ steps.vars.outputs.release_version }}
