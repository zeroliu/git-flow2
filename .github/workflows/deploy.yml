name: Deploy a build

on:
  push:
  workflow_dispatch:
    inputs:
      tag:
        required: true
        type: string

jobs:
  prep:
    uses: ./.github/workflows/zeffoEnv.yml

  test:
    runs-on: ubuntu-18.04
    needs: prep
    steps:
      - run: |
          echo ${{ needs.prep.outputs.ref }}
          echo ${{ needs.prep.outputs.environment }}
          echo ${{ needs.prep.outputs.release_version }}
